import {Typography} from "@mui/material";

const ServerError = () => {
    return <Typography variant={"h3"} align="center" sx={{my: 1, mx: 'auto'}}>Server error</Typography>
}
export default ServerError;
